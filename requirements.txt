# Streamlit Cloud defaults to Python 3.14 — RDKit wheels may be unavailable.
# Pipeline uses deterministic fallback descriptors when RDKit is not installed.
setuptools>=69.0.0
numpy>=2.0.0
pandas>=2.2.0
scikit-learn>=1.5.0
python-docx>=1.1.2
plotly>=5.24.0
